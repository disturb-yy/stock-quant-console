# AGENTS.md 

> 目标是提供**高价值索引和执行约束**，而不是复制整份设计规范。

```md
## Frontend UI

涉及 UI 工作时，按以下顺序读取：

1. `design-system/DESIGN.md`
2. `design-system/pages/<page>.md`（若存在）
3. `design-system/MASTER.md`
4. `design-system/frontend-ui-guidelines-revised.md`

`design-system/DESIGN.md` 是仓库的视觉样式基础，定义默认的视觉方向、层级、布局节奏与组件模式；日常 UI 工作必须读取。`MASTER.md` 将其转化为量策可执行的 token 与本地字体映射，页面文档只补充页面目标、信息架构、状态和已明确记录的偏离。任何来自外部研究的商标、字体、文案、媒体或专有资产都只能作为风格证据，不能直接复制到实现中。

默认视觉方向：

- 克制的数据研究终端
- 数据与任务优先
- 深色中性研究画布
- 平面表面 + 细分隔线 + 清晰排版
- 主品牌色只用于主 CTA、关键选中和少量关键数值

禁止默认生成：

- 大面积渐变
- 霓虹 / 光晕
- 玻璃拟态
- 装饰性 3D
- 巨型圆角
- 漂浮卡片瀑布流
- 无数据意义的“AI 感”动画

执行规则：

- 普通 UI 实现自主完成，不因多个可行方案重复请求确认。
- 页面文档不存在时，继续按 `DESIGN.md`、`MASTER.md` 和通用指南执行。
- 用户可见文案默认使用中文；软件名、API 路径、代码和数据库字段等技术标识可保留英文，并在需要面向用户解释时提供中文含义。
- 不新增未经批准的全局 token 或页面私有视觉体系。
- 只有修改品牌方向、全局 token、全局字体/字号/圆角/阴影/间距、组件体系时才需要明确审批。
- UI 实现任务必须实际完成页面/组件、自检响应式与状态，不得只分析后结束。
```

---

## 给 Agent 的一句话原则

> **优先自主完成日常 UI；严格遵守现有设计系统；不要为了不确定的小实现细节停下来确认；不要为了“好看”创造新的视觉语言；任何品牌级或全局设计系统变更仍必须获得明确批准。**
